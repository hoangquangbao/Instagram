import SwiftUI

enum OnScreen {
    case add_email
    case add_confirmation_code
    case add_full_name
    case add_password
    case add_birthday
    case add_username
    case signup_account
    case find_friend
    case add_photo
    case share_photo
}

enum ImageFor {
    case for_avatar
    case for_post
}
